- name: stacks
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://postgres:postgres@postgres:5432/postgres
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include stacks/tables/tables.yaml"
